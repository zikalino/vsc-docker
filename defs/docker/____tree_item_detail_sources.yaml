- item-types:
    - docker-image
  cmd: docker image inspect ${id} --format json
  refresh: self

- item-types:
    - docker-container
  cmd: docker container inspect ${id} --format json
  refresh: self
